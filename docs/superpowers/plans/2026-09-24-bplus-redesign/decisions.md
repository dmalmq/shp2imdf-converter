# Decision trail

Back to [overview](overview.md).

| Date | Decision | Options | Choice | Reason |
|---|---|---|---|---|
| 2026-09-24 | Direction | Concepts A, B, C | B+ (B with A's top bar, logo, search) | Daniel's pick |
| 2026-09-24 | Artwork colour | signal orange, plum | Plum | Daniel: "I like the plum" |
| 2026-09-24 | Residuals | 3 pairs always, 2 when scale locked | 2 when locked | Locked scale leaves 3 unknowns, so 2 pairs over-determine the fit; Daniel approved |
| 2026-09-24 | Identity | "Working as" chip, none | None; sessions only | No logins exist |
| 2026-09-24 | Delivery strategy | incremental, big-bang branch, /v2 app | Incremental on main | Shippable, verifiable steps; one UI on the shared PC |
| 2026-09-24 | Verification harness | Playwright specs, repo driver | Extend `control.mjs` | It already launches and drives every flow; no specs exist |
| 2026-09-24 | Warning colour | keep #69's tokens, switch to B's ochre | Keep #69 (`warning` #CA8A04, `warning-foreground` #854D0E), adopt B's tint #F3E6C4 for warning surfaces | B's ochre text #8A5A0A and #69's #854D0E are near-identical; keeping them avoids reversing a same-day decision Daniel approved |
| 2026-09-25 | Project lifetimes | keep today's 24 h / 50 sessions and 2 h / 20 conversions, longer idle limits with a cap | 30 days after last opened, cap 200 per flow (sessions; conversions), nothing opened in the last 24 h ever evicted (the store overflows and logs instead) | Daniel's call: a hub is only useful if projects outlive a day; 200 real sessions is an estimated 2–5 GB, and the 24 h floor means the cap can never take work in progress. Legacy env vars still win when set, with a startup warning |
| 2026-09-25 | Artwork on the hub before `/a/:id` | list artwork cards opening a blank `/illustrator`, list shapefile projects only | Shapefile projects only (`GET /api/projects?flow=shapefiles`) until phase 14; the "From Illustrator artwork" route and `.ai`/`.pdf` drop routing stay | A card whose Continue cannot reopen its project is a card that does not open; phase 14 adds artwork cards together with the route that resumes them |
| 2026-09-25 | Bring in for new work | the plan's `/shapefiles/new`, `/p/new` | `/p/new` renders Bring in with no project (the store switches to none); `/p/:id/bring-in` stays the existing project's Bring in | Phase 5 fixed the shapefile routes as `/p/:id/<stage>`, so the new-work path sits beside them; `/p/new` redirected to `/` until the hub needed somewhere to send "From floor shapefiles" |
| 2026-09-25 | Hub drop-zone routing | ask the user, guess per file, route the whole drop | Route the whole drop: `.shp .dbf .shx .prj .cpg .qix .gpkg` → Bring in; one `.ai`/`.pdf` → artwork; `.imdf`, `*.imdf.zip`, or a zip whose central directory has `manifest.json` at the root (or under its single top folder) and no `.shp` → IMDF import; any other zip, including one holding both `manifest.json` and a `.shp`, → Bring in. Unknown files are left out and named in a notice; mixed routes, or two artworks/archives, are refused | An IMDF archive never carries a `.shp`, and Bring in reports on any zip it cannot read, so the ambiguous cases fall to the route that explains itself. The files ride in history state once and are then cleared, so Back or reload does not re-run them |
| 2026-09-25 | Where an import lands | straight to Set up as before, a project's Bring in | A standard import lands on `/p/:id/bring-in`, which shows what was read and holds Continue to Set up until no file needs a decision; an IMDF-schema import still lands on Check. The hub's Continue and `/p/:id` open a project that has only been brought in (no Set up project saved yet) at Bring in | Figma's Bring in (99:4, node 104:991) is where detection results are checked, and its Needs you files are what the hub card counts; sending such a project to Set up would skip them |
| 2026-09-25 | Bring in design gaps | add endpoints and fields, show what the API returns | No "Leave it out" after import (no endpoint removes a file from a session; before import it removes the file from the queue); no "read as Shift-JIS" pill (the encoding is only in per-file warning text); the "we'll remember it for this project" copy is dropped (a type choice only suggests a learned keyword) | Phase 8 is a presentation restructure; backend scope is not added silently. Each is a candidate for a later phase |
| 2026-09-25 | Set up design source | a B+ Set up frame, Concept B, the v2 wizard frames | `v2 · Wizard` (85:4): Project & venue 86:2849, its 日本語 twin 86:3199, Summary · blocked 87:146, drawn in B+ tokens | Neither B+ (110:2, light or 121:* dark) nor Concept B (99:4) has a Set up frame; the v2 frames are the only design for these sections and carry the plan's asks (code hints, Open/Closed, More address fields). Field labels stay sentence-case sans in both languages rather than v2's mono caps: Geist Mono has no CJK, and B+'s Bring in uses sans labels |
| 2026-09-25 | File Classification in Set up | keep it, keep a read-only copy, remove it | Removed; Set up is Project & venue, Level mapping, Attribute mapping, Summary & generate | Bring in already reads and fixes file types, and its copy says any guess can be changed "now or later". What only the section did moved to Bring in: changing the type of a file that looks right (every row now has a type menu), Re-detect all ("Guess the types again"), the learning-keyword suggestion, and the preview map with hover and click-to-isolate. Its confidence and geometry columns are what Needs you / Look right and the shapes count already say. Summary's checklist keeps "Every file has a type", and its Fix opens Bring in |
| 2026-09-25 | Set up design gaps | add endpoints, show what exists | No backend change. Language is a menu of ja, en, zh-Hans, zh-Hant, ko plus any stored tag, and Other… takes any BCP 47 tag, canonicalised by `Intl.getCanonicalLocales` (there is no language list API). Country names come from `Intl.DisplayNames` over the ISO 3166-1 codes (there is no country API). Copy Monday to all is disabled while Monday is closed: the OSM-style `venue_hours` string has no closed token the client writes, so all-closed would save as null, the same as no hours given. The learning suggestion is worded in the client from its fields, since the backend `message` is English only. No dark Set up frame exists to compare against | Phase 9 is a presentation restructure; each is a candidate for a later phase |
| 2026-09-25 | Summary read-back of venue, address and language | keep the Project table, a compact read-back, none | None | Frame 87:146's own note removes the Project table: its values repeat what the checklist and Project & venue already show. The checklist names each missing venue and address field, and Fix opens it |
| 2026-09-25 | Illustrator workflow review (illustrator-review-2026-09-25.md) | adopt all of it, adopt the correctness and flow findings, defer it | Scale-scope bug ships now as its own fix; per-floor status, export checklist with one confirmation, recommended Align action, visible lookup failure, draft resume at `/a/:id` and template-mismatch-before-apply join phase 14; nudge steps, coordinate labels and fit guidance follow it; split-view picking, exact point coordinates, check points, per-level opacity and assignment-edit previews are deferred | Daniel's call on 2026-09-25. The adopted items fix wrong or silent results; a compulsory per-floor "mark reviewed" click was dropped because it adds a step without adding certainty |
| 2026-09-25 | What Check calls must fix | the design's examples (overlaps and loose doors as must fix), the validator's severities | Must fix is the validator's errors and Can wait its warnings; overlaps and doors off a wall are warnings, so they are Can wait rows whose Resolve / Snap open the same popover | The hub's blockers, the Deliver badge and the stage are all the stored error count; promoting warnings in the rail alone would make the rail disagree with all three, and changing validation rules is out of scope |
| 2026-09-25 | Undo for Check's fixes | per-feature PATCH with the prior geometry, a server-side undo stack, the fix returning its own inverse | Each fix (resolve, fix-safe, snap, auto-fix) returns `undo`: the touched features as they were and the ids it created; Undo sends it back through the bulk feature PATCH as `restore`, which revalidates | A PATCH cannot bring back a unit an overlap fix deleted, nor ids auto-fix renumbered; the inverse is computed where the change is made, needs no stored state, and a restore that changes nothing is not an edit (#77). Each undo carries a fingerprint of what the fix left per touched id and a seal over the payload: a restore is refused (409 `UNDO_STALE`) once any of those features changed, by an edit, a merge or another tab, and rejected (400 `UNDO_INVALID`) when it names ids the fix did not touch, so it only ever puts back the fix's own rows. The client also withdraws Undo after any other edit in the tab. The Done list lasts for the visit |
| 2026-09-25 | Check design gaps | follow 117:2 exactly, adapt | The issue card docks at the map's top right under the toolbar, with a numbered pin at the issue, instead of hanging off a leader line; one Auto-fix all for the whole list (the endpoint has no per-check filter); no time estimate; the stored validation is read on arrival (`GET /validation`) and the checker runs only when it is stale | A card anchored to the geometry ran off the map for long platform pairs; the others would need backend scope or an invented number |
| 2026-09-25 | Deliver's outputs and action | keep one format per export, tick several and bundle them into one zip, tick several and download each | Tick several, as frame 115:470 does; the footer's one action downloads each chosen output in turn through its own export endpoint, with the dialog's request bodies. Opens with the old default ticked (IMDF, or ODC 2026 for an IMDF-schema import), not the frame's three | A bundle would change what every download is; one file per output keeps each byte-identical to before (checked with `tools/compare_exports.py` against b00568d's exports). Each export still records the delivery, so `delivered.format` is the last output created |
| 2026-09-25 | Deliver's file tree | name files from the formats' naming rules in the client, show the frame's illustrative lines, list what each export builds | `GET /api/session/{id}/export/contents` builds each archive the way its export does and returns the download name and entries (and the rows ODC leaves out), saving nothing and recording no delivery; QGIS is the ODC listing plus its project file, without running QGIS | ODC's per-floor names depend on floor tokens, levels sharing a floor and empty layers being skipped; a second copy of those rules in TypeScript would drift. Building the archives takes about a second for the Tokyo fixture |
| 2026-09-25 | Deliver design gaps | add storage and settings, show what exists | No handover note (nothing stores one; phase 13 adds it with the project note); no "Creates 3 files in Documents › shp2imdf › 東京駅" (the browser decides where downloads go), so the footer counts the downloads; the coordinate line names each chosen output's real system (IMDF WGS 84, ODC and QGIS JGD2011 EPSG:6668, round-trip shapefiles their own) instead of a plane-rectangular zone "picked from where the station sits", which no shapefile export does; no dated root folder in the tree; "Every floor file is written once" and "N of M spaces included" are not shown (no count is returned), the rows ODC leaves out are | Phase 11 is a presentation restructure; backend scope is the read-only listing only |
