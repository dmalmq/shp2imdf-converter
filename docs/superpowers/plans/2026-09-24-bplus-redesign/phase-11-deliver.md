# Deliver

Back to [overview](overview.md).

**Goal.** Deliver becomes a stage page (Figma 115:470): outputs grouped by audience, a "What
you'll get" tree, coordinate system, last checks, handover note; one primary action (the
footer), not two.

**Verification.** Live: every format still downloads the same bytes as before (hash compare
against baseline exports from phase 1).
